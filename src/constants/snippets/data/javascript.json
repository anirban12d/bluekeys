{
  "language": "javascript",
  "groups": [[0, 100], [101, 300], [301, 600], [601, 9999]],
  "snippets": [
    { "id": 1, "text": "const sum = (a, b) => a + b;", "source": "Arrow Function", "length": 28 },
    { "id": 2, "text": "const filtered = arr.filter(item => item.active === true);", "source": "Array Filter", "length": 58 },
    { "id": 3, "text": "async function fetchData(url) { const res = await fetch(url); return res.json(); }", "source": "Async Fetch", "length": 83 },
    { "id": 4, "text": "const { name, age, ...rest } = person;", "source": "Destructuring", "length": 39 },
    { "id": 5, "text": "document.querySelector('.btn').addEventListener('click', (e) => { e.preventDefault(); console.log('clicked'); });", "source": "Event Listener", "length": 114 },
    { "id": 6, "text": "const map = new Map(); map.set('key', 'value'); console.log(map.get('key'));", "source": "Map Usage", "length": 76 },
    { "id": 7, "text": "try { const data = JSON.parse(rawInput); } catch (err) { console.error('Parse failed:', err.message); }", "source": "JSON Parse", "length": 105 },
    { "id": 8, "text": "const debounce = (fn, delay) => { let timer; return (...args) => { clearTimeout(timer); timer = setTimeout(() => fn(...args), delay); }; };", "source": "Debounce", "length": 144 },
    { "id": 9, "text": "export default class EventEmitter { constructor() { this.events = {}; } on(event, fn) { (this.events[event] ||= []).push(fn); } emit(event, ...args) { (this.events[event] || []).forEach(fn => fn(...args)); } }", "source": "Event Emitter", "length": 220 },
    { "id": 10, "text": "const unique = [...new Set(array)];", "source": "Unique Values", "length": 35 },
    { "id": 11, "text": "Promise.all([fetchUsers(), fetchPosts()]).then(([users, posts]) => { console.log(users, posts); });", "source": "Promise.all", "length": 100 },
    { "id": 12, "text": "const result = items.reduce((acc, item) => { acc[item.category] = (acc[item.category] || 0) + 1; return acc; }, {});", "source": "Reduce", "length": 115 },
    { "id": 13, "text": "function* range(start, end, step = 1) { for (let i = start; i < end; i += step) { yield i; } }", "source": "Generator", "length": 97 },
    { "id": 14, "text": "const proxy = new Proxy(target, { get(obj, prop) { return prop in obj ? obj[prop] : 'default'; } });", "source": "Proxy", "length": 101 },
    { "id": 15, "text": "class LinkedList { constructor() { this.head = null; this.size = 0; } add(value) { const node = { value, next: this.head }; this.head = node; this.size++; } }", "source": "Linked List", "length": 166 },
    { "id": 16, "text": "const flatten = (arr) => arr.reduce((flat, item) => flat.concat(Array.isArray(item) ? flatten(item) : item), []);", "source": "Array Flatten", "length": 114 },
    { "id": 17, "text": "import { readFile } from 'node:fs/promises'; const data = await readFile('./config.json', 'utf-8');", "source": "Node File Read", "length": 100 },
    { "id": 18, "text": "const deepClone = (obj) => JSON.parse(JSON.stringify(obj));", "source": "Deep Clone", "length": 59 },
    { "id": 19, "text": "const throttle = (fn, limit) => { let inThrottle; return (...args) => { if (!inThrottle) { fn(...args); inThrottle = true; setTimeout(() => inThrottle = false, limit); } }; };", "source": "Throttle", "length": 181 },
    { "id": 20, "text": "const pipe = (...fns) => (x) => fns.reduce((v, f) => f(v), x);", "source": "Pipe", "length": 63 },
    { "id": 21, "text": "for await (const chunk of stream) { process.stdout.write(chunk); }", "source": "Async Iterator", "length": 66 },
    { "id": 22, "text": "const memoize = (fn) => { const cache = new Map(); return (...args) => { const key = JSON.stringify(args); if (!cache.has(key)) cache.set(key, fn(...args)); return cache.get(key); }; };", "source": "Memoize", "length": 190 },
    { "id": 23, "text": "Object.entries(obj).forEach(([key, value]) => { console.log(`${key}: ${value}`); });", "source": "Object Entries", "length": 85 },
    { "id": 24, "text": "const sleep = (ms) => new Promise(resolve => setTimeout(resolve, ms));", "source": "Sleep Promise", "length": 70 },
    { "id": 25, "text": "class Queue { #items = []; enqueue(item) { this.#items.push(item); } dequeue() { return this.#items.shift(); } get length() { return this.#items.length; } }", "source": "Queue Class", "length": 162 },
    { "id": 26, "text": "const observer = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) { entry.target.classList.add('visible'); } }); });", "source": "Intersection Observer", "length": 164 },
    { "id": 27, "text": "const groupBy = (arr, key) => arr.reduce((groups, item) => ({ ...groups, [item[key]]: [...(groups[item[key]] || []), item] }), {});", "source": "Group By", "length": 130 },
    { "id": 28, "text": "navigator.clipboard.writeText(text).then(() => console.log('Copied'));", "source": "Clipboard API", "length": 70 },
    { "id": 29, "text": "const controller = new AbortController(); fetch(url, { signal: controller.signal }).catch(err => { if (err.name === 'AbortError') console.log('Aborted'); });", "source": "Abort Controller", "length": 160 },
    { "id": 30, "text": "structuredClone(complexObject);", "source": "Structured Clone", "length": 31 }
  ]
}
