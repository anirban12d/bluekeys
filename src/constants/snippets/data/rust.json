{
  "language": "rust",
  "groups": [[0, 100], [101, 300], [301, 600], [601, 9999]],
  "snippets": [
    { "id": 1, "text": "fn main() { println!(\"Hello, world!\"); }", "source": "Hello World", "length": 41 },
    { "id": 2, "text": "let mut vec = Vec::new(); vec.push(42);", "source": "Vec Push", "length": 39 },
    { "id": 3, "text": "match result { Ok(val) => println!(\"{}\", val), Err(e) => eprintln!(\"Error: {}\", e), }", "source": "Match Result", "length": 87 },
    { "id": 4, "text": "let filtered: Vec<_> = items.iter().filter(|x| x.active).collect();", "source": "Iterator Filter", "length": 67 },
    { "id": 5, "text": "struct Point { x: f64, y: f64 }", "source": "Struct", "length": 31 },
    { "id": 6, "text": "impl Display for Point { fn fmt(&self, f: &mut Formatter) -> fmt::Result { write!(f, \"({}, {})\", self.x, self.y) } }", "source": "Display Trait", "length": 117 },
    { "id": 7, "text": "fn read_file(path: &str) -> Result<String, io::Error> { fs::read_to_string(path) }", "source": "File Read", "length": 83 },
    { "id": 8, "text": "let handle = thread::spawn(|| { println!(\"hello from thread\"); }); handle.join().unwrap();", "source": "Thread Spawn", "length": 91 },
    { "id": 9, "text": "use std::collections::HashMap; let mut map = HashMap::new(); map.insert(\"key\", \"value\");", "source": "HashMap", "length": 89 },
    { "id": 10, "text": "enum Command { Quit, Echo(String), Move { x: i32, y: i32 }, }", "source": "Enum", "length": 62 },
    { "id": 11, "text": "if let Some(value) = optional { println!(\"Got: {}\", value); }", "source": "If Let", "length": 61 },
    { "id": 12, "text": "let sum: i32 = (1..=100).sum();", "source": "Range Sum", "length": 31 },
    { "id": 13, "text": "fn generic<T: Display + Clone>(item: T) -> String { format!(\"{}\", item) }", "source": "Generics", "length": 74 },
    { "id": 14, "text": "let (tx, rx) = mpsc::channel(); thread::spawn(move || { tx.send(42).unwrap(); }); println!(\"{}\", rx.recv().unwrap());", "source": "Channel", "length": 118 },
    { "id": 15, "text": "impl Iterator for Counter { type Item = u32; fn next(&mut self) -> Option<Self::Item> { self.count += 1; if self.count <= 5 { Some(self.count) } else { None } } }", "source": "Iterator Impl", "length": 166 },
    { "id": 16, "text": "#[derive(Debug, Clone, Serialize, Deserialize)] struct Config { host: String, port: u16, }", "source": "Derive Macros", "length": 91 },
    { "id": 17, "text": "let data = Arc::new(Mutex::new(vec![1, 2, 3]));", "source": "Arc Mutex", "length": 48 },
    { "id": 18, "text": "async fn fetch(url: &str) -> Result<String, reqwest::Error> { let body = reqwest::get(url).await?.text().await?; Ok(body) }", "source": "Async Fetch", "length": 122 },
    { "id": 19, "text": "fn fibonacci(n: u64) -> u64 { match n { 0 => 0, 1 => 1, _ => fibonacci(n - 1) + fibonacci(n - 2), } }", "source": "Fibonacci", "length": 103 },
    { "id": 20, "text": "let closure = |x: i32, y: i32| -> i32 { x + y };", "source": "Closure", "length": 50 },
    { "id": 21, "text": "trait Animal { fn name(&self) -> &str; fn sound(&self) -> &str; fn info(&self) -> String { format!(\"{} says {}\", self.name(), self.sound()) } }", "source": "Trait", "length": 151 },
    { "id": 22, "text": "let content = fs::read_to_string(\"input.txt\")?;", "source": "Question Mark", "length": 48 },
    { "id": 23, "text": "use clap::Parser; #[derive(Parser)] struct Cli { #[arg(short, long)] name: String, #[arg(short, long, default_value_t = 8080)] port: u16, }", "source": "Clap CLI", "length": 148 },
    { "id": 24, "text": "let numbers = vec![1, 2, 3, 4, 5]; let doubled: Vec<i32> = numbers.iter().map(|x| x * 2).collect();", "source": "Map Collect", "length": 100 },
    { "id": 25, "text": "Box::new(value)", "source": "Box Heap", "length": 15 },
    { "id": 26, "text": "fn longest<'a>(x: &'a str, y: &'a str) -> &'a str { if x.len() > y.len() { x } else { y } }", "source": "Lifetimes", "length": 93 },
    { "id": 27, "text": "macro_rules! vec_of_strings { ($($x:expr),*) => { vec![$($x.to_string()),*] }; }", "source": "Macro Rules", "length": 80 },
    { "id": 28, "text": "let guard = mutex.lock().unwrap(); drop(guard);", "source": "Mutex Lock", "length": 47 },
    { "id": 29, "text": "#[cfg(test)] mod tests { use super::*; #[test] fn it_works() { assert_eq!(add(2, 2), 4); } }", "source": "Unit Test", "length": 95 },
    { "id": 30, "text": "std::env::args().collect::<Vec<String>>()", "source": "Env Args", "length": 41 }
  ]
}
